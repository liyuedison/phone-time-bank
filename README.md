# 薪火传承成交播报

这是一个现场成交播报页面原型。

- `index.html`：后台录入页，可添加地区、时间、姓名、手机号、成交单数、备注。
- `display.html`：对外展示页，只显示成交播报大屏。
- `styles.css`：页面样式。
- `app.js`：数据渲染和本地保存逻辑。

当前数据保存在浏览器本地 `localStorage`，适合单台设备现场展示。若需要多人在线录入和统一展示，需要接入数据库和真正的后台服务。
